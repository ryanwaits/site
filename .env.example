# Required: Anthropic API key for Claude Agent SDK
ANTHROPIC_API_KEY=sk-ant-...

# Optional: Override default port (default: 3000)
# PORT=3000

# Optional: Override hostname (default: localhost)
# HOSTNAME=0.0.0.0
